name: index_ruleeETsyNSJmhuh
runtime: yaml
description: A minimal Pulumi program to create an Auth0 rule and its dependencies
config:
  pulumi:tags:
    value:
      pulumi:template: https://www.pulumi.com/ai/api/project/9f832c9b-ef05-4009-8661-e53dcf7f1b10.zip
resources:
  myAuth0Rule:
    properties:
      enabled: true
      name: my-test-rule
      order: 1
      script: |
        function(user, context, callback) {
          // This is just a placeholder rule that doesn't change anything
          callback(null, user, context);
        }
    type: auth0:Rule
