name: index_action4cOIUu5LiJiV
runtime: yaml
description: A minimal valid Pulumi program to create an Auth0 Action with dependencies
config:
  pulumi:tags:
    value:
      pulumi:template: https://www.pulumi.com/ai/api/project/be493b8c-5de2-49d7-986f-69993f1eb51c.zip
resources:
  myAction:
    properties:
      code: |
        exports.onExecutePostLogin = async (event, api) => {
          // Write your custom Action code here
        }
      name: my-custom-action
      runtime: node12
      supportedTriggers:
        - id: post-login
          version: v1
    type: auth0:Action
