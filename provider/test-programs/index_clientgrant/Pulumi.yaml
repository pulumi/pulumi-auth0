name: index_clientgrant5gx7ji8aTIOd
runtime: yaml
description: ""
config:
  pulumi:tags:
    value:
      pulumi:template: https://www.pulumi.com/ai/api/project/de37f3a1-0802-4e79-8b81-74feddb6e4dc.zip
outputs:
  clientGrantId: ${myAuth0ClientGrant.id}
  clientId: ${myAuth0Client.clientId}
resources:
  myAuth0Client:
    properties:
      grantTypes:
        - authorization_code
        - refresh_token
        - client_credentials
      name: My Auth0 Client
      resourceServers: []
    type: auth0:Client
  myAuth0ClientGrant:
    properties:
      audience: https://api.example.com/
      clientId: ${myAuth0Client.clientId}
      scopes:
        - read:messages
        - openid
    type: auth0:ClientGrant
