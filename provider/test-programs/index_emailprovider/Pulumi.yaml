name: index_emailprovidermwexJlP84qLa
runtime: yaml
description: ""
config:
  pulumi:tags:
    value:
      pulumi:template: https://www.pulumi.com/ai/api/project/8120eef2-c718-4b53-acb6-e93c0f6ac543.zip
resources:
  auth0EmailProvider:
    properties:
      credentials:
        apiKey: your-api-key-here
        domain: mail.example.com
        smtpHost: smtp.example.com
        smtpPass: smtp-password
        smtpPort: 587
        smtpUser: email-user
      defaultFromAddress: no-reply@example.com
      enabled: true
      name: my-email-provider
    type: auth0:Email
