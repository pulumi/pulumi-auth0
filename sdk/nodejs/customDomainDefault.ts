// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * With this resource, you can configure the default domain for your Auth0 tenant. The default domain is the domain that Auth0 will use for various tenant-level operations. This resource manages the default domain configuration via the custom domains API.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as auth0 from "@pulumi/auth0";
 *
 * const myCustomDomain = new auth0.CustomDomain("my_custom_domain", {
 *     domain: "auth.example.com",
 *     type: "auth0_managed_certs",
 * });
 * const _default = new auth0.CustomDomainDefault("default", {domain: myCustomDomain.domain});
 * ```
 *
 * ## Import
 *
 * As this is a singleton resource (one per tenant), it can be imported using the
 * fixed identifier "customDomainDefault".
 *
 * Example:
 *
 * ```sh
 * $ pulumi import auth0:index/customDomainDefault:CustomDomainDefault default "custom_domain_default"
 * ```
 */
export class CustomDomainDefault extends pulumi.CustomResource {
    /**
     * Get an existing CustomDomainDefault resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: CustomDomainDefaultState, opts?: pulumi.CustomResourceOptions): CustomDomainDefault {
        return new CustomDomainDefault(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'auth0:index/customDomainDefault:CustomDomainDefault';

    /**
     * Returns true if the given object is an instance of CustomDomainDefault.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is CustomDomainDefault {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === CustomDomainDefault.__pulumiType;
    }

    /**
     * The custom domain name or canonical domain name to set as the default domain for the tenant.
     */
    declare public readonly domain: pulumi.Output<string>;

    /**
     * Create a CustomDomainDefault resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: CustomDomainDefaultArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: CustomDomainDefaultArgs | CustomDomainDefaultState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as CustomDomainDefaultState | undefined;
            resourceInputs["domain"] = state?.domain;
        } else {
            const args = argsOrState as CustomDomainDefaultArgs | undefined;
            if (args?.domain === undefined && !opts.urn) {
                throw new Error("Missing required property 'domain'");
            }
            resourceInputs["domain"] = args?.domain;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(CustomDomainDefault.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering CustomDomainDefault resources.
 */
export interface CustomDomainDefaultState {
    /**
     * The custom domain name or canonical domain name to set as the default domain for the tenant.
     */
    domain?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a CustomDomainDefault resource.
 */
export interface CustomDomainDefaultArgs {
    /**
     * The custom domain name or canonical domain name to set as the default domain for the tenant.
     */
    domain: pulumi.Input<string>;
}
