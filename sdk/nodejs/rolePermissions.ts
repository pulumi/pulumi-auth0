// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * With this resource, you can manage role permissions (1-many).
 *
 * !> This resource manages all the permissions assigned to a role. In contrast, the `auth0.RolePermission` resource only
 * appends a permission to a role. To avoid potential issues, it is recommended not to use this resource in conjunction
 * with the `auth0.RolePermission` resource when managing permissions for the same role id.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as auth0 from "@pulumi/auth0";
 *
 * // Example:
 * const resourceServer = new auth0.ResourceServer("resource_server", {
 *     name: "test",
 *     identifier: "test.example.com",
 * });
 * const resourceServerScopes = new auth0.ResourceServerScopes("resource_server_scopes", {
 *     resourceServerIdentifier: resourceServer.identifier,
 *     scopes: [
 *         {
 *             name: "store:create",
 *         },
 *         {
 *             name: "store:read",
 *         },
 *         {
 *             name: "store:update",
 *         },
 *         {
 *             name: "store:delete",
 *         },
 *     ],
 * });
 * const myRole = new auth0.Role("my_role", {name: "My Role"});
 * const myRolePerms = new auth0.RolePermissions("my_role_perms", {
 *     permissions: resourceServer.identifier.apply(identifier => .map(entry => ({
 *         name: entry.value.name,
 *         resourceServerIdentifier: identifier,
 *     }))),
 *     roleId: myRole.id,
 * });
 * ```
 *
 * ## Import
 *
 * This resource can be imported by specifying the role ID
 *
 * Example:
 *
 * ```sh
 * $ pulumi import auth0:index/rolePermissions:RolePermissions all_role_permissions "rol_XXXXXXXXXXXX"
 * ```
 */
export class RolePermissions extends pulumi.CustomResource {
    /**
     * Get an existing RolePermissions resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: RolePermissionsState, opts?: pulumi.CustomResourceOptions): RolePermissions {
        return new RolePermissions(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'auth0:index/rolePermissions:RolePermissions';

    /**
     * Returns true if the given object is an instance of RolePermissions.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is RolePermissions {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === RolePermissions.__pulumiType;
    }

    /**
     * List of API permissions granted to the role.
     */
    public readonly permissions!: pulumi.Output<outputs.RolePermissionsPermission[]>;
    /**
     * ID of the role to associate the permission to.
     */
    public readonly roleId!: pulumi.Output<string>;

    /**
     * Create a RolePermissions resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: RolePermissionsArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: RolePermissionsArgs | RolePermissionsState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as RolePermissionsState | undefined;
            resourceInputs["permissions"] = state ? state.permissions : undefined;
            resourceInputs["roleId"] = state ? state.roleId : undefined;
        } else {
            const args = argsOrState as RolePermissionsArgs | undefined;
            if ((!args || args.permissions === undefined) && !opts.urn) {
                throw new Error("Missing required property 'permissions'");
            }
            if ((!args || args.roleId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'roleId'");
            }
            resourceInputs["permissions"] = args ? args.permissions : undefined;
            resourceInputs["roleId"] = args ? args.roleId : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(RolePermissions.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering RolePermissions resources.
 */
export interface RolePermissionsState {
    /**
     * List of API permissions granted to the role.
     */
    permissions?: pulumi.Input<pulumi.Input<inputs.RolePermissionsPermission>[]>;
    /**
     * ID of the role to associate the permission to.
     */
    roleId?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a RolePermissions resource.
 */
export interface RolePermissionsArgs {
    /**
     * List of API permissions granted to the role.
     */
    permissions: pulumi.Input<pulumi.Input<inputs.RolePermissionsPermission>[]>;
    /**
     * ID of the role to associate the permission to.
     */
    roleId: pulumi.Input<string>;
}
