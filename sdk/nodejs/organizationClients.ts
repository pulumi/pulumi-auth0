// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * With this resource, you can manage all of the client (application) associations of an organization, controlling those applications' entitlement to the organization (EA only). This resource is authoritative: it manages the full set of associations, so it must not be used together with the `auth0.OrganizationClient` resource on the same organization. An organization accepts up to 100 associated clients.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as auth0 from "@pulumi/auth0";
 *
 * const myOrganization = new auth0.Organization("my_organization", {
 *     name: "my-organization",
 *     displayName: "My Organization",
 * });
 * const myClient = new auth0.Client("my_client", {name: "My Application"});
 * const myOtherClient = new auth0.Client("my_other_client", {name: "My Other Application"});
 * // Entitle both applications to the organization in one authoritative resource (EA only).
 * // Use this instead of auth0_organization_client when you want Terraform to own the full
 * // set of associations; do not use both resources on the same organization.
 * const myOrgClients = new auth0.OrganizationClients("my_org_clients", {
 *     organizationId: myOrganization.id,
 *     clients: [
 *         {
 *             clientId: myClient.id,
 *             useForMemberAccess: true,
 *         },
 *         {
 *             clientId: myOtherClient.id,
 *         },
 *     ],
 * });
 * ```
 *
 * ## Import
 *
 * This resource can be imported by specifying the organization ID.
 *
 * Example:
 *
 * ```sh
 * $ pulumi import auth0:index/organizationClients:OrganizationClients my_org_clients "org_XXXXX"
 * ```
 */
export class OrganizationClients extends pulumi.CustomResource {
    /**
     * Get an existing OrganizationClients resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: OrganizationClientsState, opts?: pulumi.CustomResourceOptions): OrganizationClients {
        return new OrganizationClients(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'auth0:index/organizationClients:OrganizationClients';

    /**
     * Returns true if the given object is an instance of OrganizationClients.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is OrganizationClients {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === OrganizationClients.__pulumiType;
    }

    /**
     * The clients (applications) associated with the organization.
     */
    declare public readonly clients: pulumi.Output<outputs.OrganizationClientsClient[]>;
    /**
     * The ID of the organization to associate the clients (applications) with.
     */
    declare public readonly organizationId: pulumi.Output<string>;

    /**
     * Create a OrganizationClients resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: OrganizationClientsArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: OrganizationClientsArgs | OrganizationClientsState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as OrganizationClientsState | undefined;
            resourceInputs["clients"] = state?.clients;
            resourceInputs["organizationId"] = state?.organizationId;
        } else {
            const args = argsOrState as OrganizationClientsArgs | undefined;
            if (args?.clients === undefined && !opts.urn) {
                throw new Error("Missing required property 'clients'");
            }
            if (args?.organizationId === undefined && !opts.urn) {
                throw new Error("Missing required property 'organizationId'");
            }
            resourceInputs["clients"] = args?.clients;
            resourceInputs["organizationId"] = args?.organizationId;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(OrganizationClients.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering OrganizationClients resources.
 */
export interface OrganizationClientsState {
    /**
     * The clients (applications) associated with the organization.
     */
    clients?: pulumi.Input<pulumi.Input<inputs.OrganizationClientsClient>[] | undefined>;
    /**
     * The ID of the organization to associate the clients (applications) with.
     */
    organizationId?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a OrganizationClients resource.
 */
export interface OrganizationClientsArgs {
    /**
     * The clients (applications) associated with the organization.
     */
    clients: pulumi.Input<pulumi.Input<inputs.OrganizationClientsClient>[]>;
    /**
     * The ID of the organization to associate the clients (applications) with.
     */
    organizationId: pulumi.Input<string>;
}
