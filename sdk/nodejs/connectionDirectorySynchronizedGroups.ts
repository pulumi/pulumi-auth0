// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * With this resource, you can manage the set of Google Workspace group IDs synchronized via directory provisioning for an Auth0 connection. (EA only)
 *
 * > This resource is only available for [EA](https://auth0.com/docs/troubleshoot/product-lifecycle/product-release-stages#early-access) users.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as auth0 from "@pulumi/auth0";
 *
 * const myConnection = new auth0.Connection("my_connection", {
 *     name: "My-Google-Workspace-Connection",
 *     strategy: "google-apps",
 *     options: {
 *         clientId: "...",
 *         clientSecret: "...",
 *         domain: "example.com",
 *         tenantDomain: "example.com",
 *         apiEnableUsers: true,
 *         apiEnableGroups: true,
 *     },
 * });
 * const myDirectory = new auth0.ConnectionDirectory("my_directory", {
 *     connectionId: myConnection.id,
 *     synchronizeGroups: "selected",
 * });
 * const myGroups = new auth0.ConnectionDirectorySynchronizedGroups("my_groups", {
 *     connectionId: myConnection.id,
 *     groupIds: [
 *         "group1abc",
 *         "group2def",
 *         "group3ghi",
 *     ],
 * }, {
 *     dependsOn: [myDirectory],
 * });
 * ```
 *
 * ## Import
 *
 * ```sh
 * $ pulumi import auth0:index/connectionDirectorySynchronizedGroups:ConnectionDirectorySynchronizedGroups my_groups "con_XXXXXXXXXXXXXX"
 * ```
 */
export class ConnectionDirectorySynchronizedGroups extends pulumi.CustomResource {
    /**
     * Get an existing ConnectionDirectorySynchronizedGroups resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ConnectionDirectorySynchronizedGroupsState, opts?: pulumi.CustomResourceOptions): ConnectionDirectorySynchronizedGroups {
        return new ConnectionDirectorySynchronizedGroups(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'auth0:index/connectionDirectorySynchronizedGroups:ConnectionDirectorySynchronizedGroups';

    /**
     * Returns true if the given object is an instance of ConnectionDirectorySynchronizedGroups.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ConnectionDirectorySynchronizedGroups {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ConnectionDirectorySynchronizedGroups.__pulumiType;
    }

    /**
     * ID of the connection for which to manage synchronized groups. (EA only)
     */
    declare public readonly connectionId: pulumi.Output<string>;
    /**
     * List of Google Workspace Directory group IDs to synchronize. (EA only)
     */
    declare public readonly groupIds: pulumi.Output<string[]>;

    /**
     * Create a ConnectionDirectorySynchronizedGroups resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ConnectionDirectorySynchronizedGroupsArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ConnectionDirectorySynchronizedGroupsArgs | ConnectionDirectorySynchronizedGroupsState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ConnectionDirectorySynchronizedGroupsState | undefined;
            resourceInputs["connectionId"] = state?.connectionId;
            resourceInputs["groupIds"] = state?.groupIds;
        } else {
            const args = argsOrState as ConnectionDirectorySynchronizedGroupsArgs | undefined;
            if (args?.connectionId === undefined && !opts.urn) {
                throw new Error("Missing required property 'connectionId'");
            }
            if (args?.groupIds === undefined && !opts.urn) {
                throw new Error("Missing required property 'groupIds'");
            }
            resourceInputs["connectionId"] = args?.connectionId;
            resourceInputs["groupIds"] = args?.groupIds;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ConnectionDirectorySynchronizedGroups.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ConnectionDirectorySynchronizedGroups resources.
 */
export interface ConnectionDirectorySynchronizedGroupsState {
    /**
     * ID of the connection for which to manage synchronized groups. (EA only)
     */
    connectionId?: pulumi.Input<string | undefined>;
    /**
     * List of Google Workspace Directory group IDs to synchronize. (EA only)
     */
    groupIds?: pulumi.Input<pulumi.Input<string>[] | undefined>;
}

/**
 * The set of arguments for constructing a ConnectionDirectorySynchronizedGroups resource.
 */
export interface ConnectionDirectorySynchronizedGroupsArgs {
    /**
     * ID of the connection for which to manage synchronized groups. (EA only)
     */
    connectionId: pulumi.Input<string>;
    /**
     * List of Google Workspace Directory group IDs to synchronize. (EA only)
     */
    groupIds: pulumi.Input<pulumi.Input<string>[]>;
}
