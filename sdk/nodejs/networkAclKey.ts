// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Manages HMAC-SHA256 signing keys used to verify HTTP Message Signatures on Network ACL rules. The key material (`value`) is write-only: it is not returned by the API and is not stored in Terraform state. Changes are detected by comparing SHA-256 fingerprints. (EA Only)
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as auth0 from "@pulumi/auth0";
 *
 * // Create an HMAC-SHA256 key for Network ACL HTTP Message Signature verification.
 * // The key material (value) is write-only and not stored in Terraform state.
 * const myKey = new auth0.NetworkAclKey("my_key", {
 *     name: "my-hmac-key",
 *     alg: "hmac-sha256",
 *     value: hmacKeyBase64,
 * });
 * // Reference the key in a Network ACL rule to allow only signed requests.
 * const allowSignedOnly = new auth0.NetworkAcl("allow_signed_only", {
 *     rule: {
 *         action: {
 *             block: true,
 *         },
 *         notMatch: {
 *             httpMessageSignature: {
 *                 keys: [{
 *                     id: myKey.id,
 *                 }],
 *             },
 *         },
 *         scope: "tenant",
 *     },
 *     description: "Block requests without a valid HMAC signature",
 *     active: true,
 *     priority: 99,
 * });
 * ```
 */
export class NetworkAclKey extends pulumi.CustomResource {
    /**
     * Get an existing NetworkAclKey resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: NetworkAclKeyState, opts?: pulumi.CustomResourceOptions): NetworkAclKey {
        return new NetworkAclKey(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'auth0:index/networkAclKey:NetworkAclKey';

    /**
     * Returns true if the given object is an instance of NetworkAclKey.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is NetworkAclKey {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === NetworkAclKey.__pulumiType;
    }

    /**
     * Algorithm used for the key. Currently only `hmac-sha256` is supported.
     */
    declare public readonly alg: pulumi.Output<string>;
    /**
     * Time when the key was created.
     */
    declare public /*out*/ readonly createdAt: pulumi.Output<string>;
    /**
     * SHA-256 fingerprint of the decoded key material (lowercase hex). Used for drift detection.
     */
    declare public /*out*/ readonly fingerprint: pulumi.Output<string>;
    /**
     * User-supplied label for the key. Must be unique across all Network ACL keys for the tenant. Max 255 characters.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * Time when the key was last updated.
     */
    declare public /*out*/ readonly updatedAt: pulumi.Output<string>;
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * Base64-encoded raw key material. The decoded value must be between 32 and 512 bytes. This field is write-only: it is not returned by the API and not stored in Terraform state. Changes are detected by comparing SHA-256 fingerprints.
     */
    declare public readonly value: pulumi.Output<string>;

    /**
     * Create a NetworkAclKey resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: NetworkAclKeyArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: NetworkAclKeyArgs | NetworkAclKeyState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as NetworkAclKeyState | undefined;
            resourceInputs["alg"] = state?.alg;
            resourceInputs["createdAt"] = state?.createdAt;
            resourceInputs["fingerprint"] = state?.fingerprint;
            resourceInputs["name"] = state?.name;
            resourceInputs["updatedAt"] = state?.updatedAt;
            resourceInputs["value"] = state?.value;
        } else {
            const args = argsOrState as NetworkAclKeyArgs | undefined;
            if (args?.alg === undefined && !opts.urn) {
                throw new Error("Missing required property 'alg'");
            }
            if (args?.value === undefined && !opts.urn) {
                throw new Error("Missing required property 'value'");
            }
            resourceInputs["alg"] = args?.alg;
            resourceInputs["name"] = args?.name;
            resourceInputs["value"] = args?.value ? pulumi.secret(args.value) : undefined;
            resourceInputs["createdAt"] = undefined /*out*/;
            resourceInputs["fingerprint"] = undefined /*out*/;
            resourceInputs["updatedAt"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["value"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(NetworkAclKey.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering NetworkAclKey resources.
 */
export interface NetworkAclKeyState {
    /**
     * Algorithm used for the key. Currently only `hmac-sha256` is supported.
     */
    alg?: pulumi.Input<string | undefined>;
    /**
     * Time when the key was created.
     */
    createdAt?: pulumi.Input<string | undefined>;
    /**
     * SHA-256 fingerprint of the decoded key material (lowercase hex). Used for drift detection.
     */
    fingerprint?: pulumi.Input<string | undefined>;
    /**
     * User-supplied label for the key. Must be unique across all Network ACL keys for the tenant. Max 255 characters.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * Time when the key was last updated.
     */
    updatedAt?: pulumi.Input<string | undefined>;
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * Base64-encoded raw key material. The decoded value must be between 32 and 512 bytes. This field is write-only: it is not returned by the API and not stored in Terraform state. Changes are detected by comparing SHA-256 fingerprints.
     */
    value?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a NetworkAclKey resource.
 */
export interface NetworkAclKeyArgs {
    /**
     * Algorithm used for the key. Currently only `hmac-sha256` is supported.
     */
    alg: pulumi.Input<string>;
    /**
     * User-supplied label for the key. Must be unique across all Network ACL keys for the tenant. Max 255 characters.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * Base64-encoded raw key material. The decoded value must be between 32 and 512 bytes. This field is write-only: it is not returned by the API and not stored in Terraform state. Changes are detected by comparing SHA-256 fingerprints.
     */
    value: pulumi.Input<string>;
}
